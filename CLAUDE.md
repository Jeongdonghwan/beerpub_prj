# CLAUDE.md — 맥주펍 프랜차이즈 가맹모집 사이트

> 벤치마크: realpub.co.kr 의 구조·레이아웃·인터랙션을 재현. 콘텐츠는 전량 자체 제작.
> 브랜드명은 임시로 **"OO펍"** 사용 — 확정 시 전역 치환 (`config/brand.py` 의 `name` 만 바꾸면 템플릿 전체 반영).
> `[확인필요]` 표시된 값은 자료 수급 후 교체. **구현은 지금 값 그대로 진행**하고 나중에 config만 수정하면 됨.

---

## 0. 프로젝트 개요

- **목적**: 맥주 전문 펍 프랜차이즈 가맹모집 사이트. 핵심 전환 = 가맹문의(리드) 접수.
- **타깃**: 소자본 창업 희망자 (외식업 초보 포함), 네이버 검색광고·플레이스 유입 중심 → 모바일 우선.
- **기술 스택**: Flask + Jinja2 SSR + MariaDB, Gunicorn + Nginx, Cafe24 가상서버 배포.
- **프레임워크 금지**: React 등 SPA 금지 (네이버 SEO 목적 SSR 유지), jQuery 신규 도입 금지, 팝업 상태는 쿠키 사용(localStorage 금지).
- **저작권**: 리얼펍의 이미지·로고·카피 원문 사용 절대 금지. 구조/효과만 재현.

## 1. 사이트맵 (IA)

```
/                       메인 (12섹션 롱스크롤)
/brand/info             브랜드 소개
/brand/history          연혁
/menu/preview           메뉴판 미리보기
/menu/list?ca_id=XX     카테고리별 메뉴 (카테고리 DB 관리)
/interior/list          매장 인테리어
/store/list             매장찾기
/startup/step           개설절차
/startup/cost           창업비용
/inquiry                창업문의
/board/notice           창업소식
/doc/policy /doc/private /doc/antiemail   약관류
/admin/*                관리자
```

### GNB (2단 드롭다운)
| 1뎁스 | 2뎁스 |
|---|---|
| 브랜드 | 브랜드, 연혁 |
| 메뉴안내 | 메뉴판 미리보기, 신메뉴, 시그니처, 튀김, 분식/피자, 탕/볶음, 마른안주, 사이드, 주류 `[확인필요: 카테고리 확정]` |
| 매장 인테리어 | — |
| 매장찾기 | — |
| 창업안내 | 개설절차, 창업비용, 창업문의, 창업소식 |
| 약관 및 정책 | 이용약관, 개인정보처리방침, 이메일무단수집거부 |

- 헤더 우측: `가맹문의 1577-0000 [확인필요]` 상시 노출, tel: 링크
- 스크롤 시 sticky + 배경 불투명 전환. 모바일: 햄버거 → 풀스크린 아코디언 메뉴

---

## 2. 메인 페이지 — 섹션별 스펙 + 카피 초안

### ★★ 핵심 레이아웃 구조 (벤치마크 사이트의 정체성 — 최우선 구현)

1. **풀페이지 스크롤 (PC ≥1025px)**: 각 섹션이 뷰포트 한 화면(100vh)을 차지하고, 휠 한 번에 한 섹션씩 전환. 구현은 CSS `scroll-snap-type: y mandatory` + 섹션별 `scroll-snap-align: start; scroll-snap-stop: always` (라이브러리 불필요, fullpage.js 금지 — 유료 라이선스). 우측 고정 도트 내비게이션으로 현재 섹션 표시.
2. **모바일 (≤1024px)**: 스냅 전부 해제 → 일반 세로 스크롤. 섹션 높이 auto.
3. **섹션 리빌 재생**: IntersectionObserver로 섹션 진입 시 `.active` 클래스 부여 → 내부 `[data-rv]` 요소 stagger 등장. **섹션 이탈 시 클래스 제거해서 재진입 때마다 애니메이션 재생** (풀페이지 특유의 효과).
4. **헤더 (화이트 고정)**: 흰 배경, 좌측 블랙 로고(2줄: 브랜드명/서브명), 중앙 GNB, 우측 "가맹문의(레드 소형)" + 대형 블랙 전화번호. 높이 ~96px.
5. **하단 고정 가맹문의 인라인 폼 바 (전 화면 상시 노출 — 핵심 전환 장치)**: 다크 배경 바에 [로고 | 가맹문의 전화 | 성함 input | 연락처 input | 가맹희망지역 select | 유입경로 select | 개인정보동의 체크 | 레드 문의하기 버튼]이 한 줄로 배치. 모달이 아니라 **인라인 폼**. 모바일에서는 [성함 | 연락처 | 문의하기]로 축약. 콘텐츠가 폼 바에 가리지 않도록 섹션 하단 패딩 확보.

### 섹션 상세

### S1. 히어로 (유튜브 배경영상)
- 풀스크린 embed: `autoplay=1&mute=1&controls=0&loop=1&playlist={ID}` / `hero_youtube_id [확인필요]`
- 영상 폴백: 풀스크린 이미지 + Ken Burns 줌
- 오버레이 카피 (페이드업 stagger):
  - 메인: **"오늘 하루 끝, 완벽한 첫 잔"**
  - 서브: "제대로 뽑은 생맥주와 제대로 만든 안주. OO펍이 동네의 밤을 바꿉니다."
- 하단 스크롤 유도 화살표 (바운스)

### S2. 브랜드 키비주얼 + "선택해야 하는 이유" 6박스 (네온 스타일)
- 좌: 손글씨풍 소카피("창업만족도 1위" `[확인필요: 근거]`) + 초대형 네온 글로우 브랜드명 + 구분선 + "OO펍을 선택해야 하는 이유"
- 우: **번호 뱃지가 상단에 얹힌 네온 보더 박스 6개 (3×2 그리드)**, stagger 등장. 박스 내 핵심 수치는 옐로 강조
  - 초안: ①고급 인테리어에 저렴한 창업비 ②3일이면 교육 끝 ③4분이면 뚝딱 맛있는 메뉴 ④광고분담금 제로 ⑤연 2회 신메뉴 출시 ⑥수상/신뢰 이력 `[확인필요]`
- 배경은 시즌 키비주얼로 교체 가능 (여름=풀장 블루, 겨울 등 — admin 배너처럼 교체 가능한 구조 권장)
- 하단 우측 각주: "※ 메뉴별 조리시간 상이할 수 있음"

### S3. 문제제기 → 해결 (대비 섹션)
리얼펍의 "2D 도면 vs 3D 렌더링" 포지션. 우리 브랜드의 대비 프레임 초안 3안 — **기본값 A안으로 구현**, 자료 확인 후 확정:
- **A안 (수익구조)**: "아직도! 본사만 배부른 프랜차이즈에 내 돈을 맡기시나요?" → "OO펍은 점주 마진 구조를 숫자로 공개합니다" `[확인필요: 공개 가능한 원가율/마진 수치]`
- B안 (오픈 후 관리): "오픈하면 끝인 본사, 겪어보셨나요?" → "OO펍은 오픈 후 정기 슈퍼바이징으로 끝까지 함께합니다"
- C안 (맥주 품질): "어디서나 똑같은 그 맥주, 손님이 다시 올까요?" → "OO펍은 매장별 관 세척·온도 관리 시스템으로 첫 잔 품질을 지킵니다"
- 구성: 상단 문제 이미지 + 도발형 헤드라인(accent span) / 하단 해결 이미지 2장 + 뱃지, 좌우 슬라이드 인 / 소형 각주 지원

### S4. 시그니처 주류 센터포커스 캐러셀 (다크 + 얼음/서리 무드)
- 다크 배경 + 얼음 파편 장식, 상단 헤드라인: "살얼음으로 <레드>시원하게</레드>" + 네온 글로우 서브 "OO펍 살얼음 주류" `[확인필요: 시그니처 콘셉트명]`
- **주류 상품컷 5종이 가로로 늘어선 센터포커스 캐러셀**: 가운데 잔이 크게(scale 1), 양옆은 작고 어둡게(scale .78 + brightness↓), 클릭/자동(3.5s)으로 전환, 하단 도트 페이지네이션 + 현재 상품명 pill
- 초안 라인업 `[확인필요]`: 살얼음 맥주 / 레몬 하이볼 / 자몽 하이볼 / 오션 하이볼 / 유자 하이볼
- 클릭 시 주류 카테고리 메뉴 페이지 이동

### S5. 운영 강점 3가지
- 헤드라인: **"간단한 레시피로 누가 만들어도 같은 맛!"**
- 서브: "요리 경험 없어도 괜찮습니다. 시스템이 맛을 만듭니다."
- 불릿 `[확인필요: 교육일수/조리시간 실측치]`:
  1. "안주가 맛있는 맥주집"
  2. "3일 교육이면 전 메뉴 마스터" 
  3. "평균 5분, 주문부터 서빙까지"

### S6. 메뉴 그리드 + 상세 모달
- 헤드라인: **"안주가 맛있는 OO펍의 메뉴"** / 서브: "가격 보고 한 번, 맛 보고 또 한 번 놀라실 겁니다"
- 대표 메뉴 10개 그리드 (호버 확대) + "메뉴전체보기" 버튼
- 클릭 → Ajax 모달 `/menu/popup/<id>` (사진/이름/가격/설명)
- 더미 메뉴 10종으로 우선 구현 `[확인필요: 실제 메뉴 리스트+사진]`

### S7. 인테리어 2열 역방향 마퀴
- 썸네일 2줄 마퀴 (윗줄 →, 아랫줄 ←) + "더보기" → /interior/list
- 카피: "매장 인테리어 — OO펍의 공간을 미리 만나보세요"
- 구현 시 무료 스톡 더미 이미지 사용 `[확인필요: 실매장 사진]`

### S8. 상권 적합성 3분할
- 헤드라인: **"언제 어디서나 한 잔, OO펍은 어느 상권에나 어울립니다"**
- 3종 (텍스트 + 배경 이미지 순차 전환):
  1. "편하게 한 잔 — 우리동네 골목 상권"
  2. "시원하게 한 잔 — 오피스·역세권 상권"
  3. "분위기 있게 한 잔 — 번화가 상권"

### S9. 성공창업 스토리
- 헤드라인: "성공창업 스토리 — 먼저 시작한 점주님들의 진짜 이야기"
- 카드 4개 (지점명 뱃지 + 썸네일 + 한줄 카피), 클릭 → Ajax 모달 전문
- 더미 4건으로 구현 `[확인필요: 실제 점주 인터뷰]` — 더미 예: "OO점 — 장사 초보였던 제가 6개월 만에 단골 가게 사장이 됐습니다"

### S10. 개설절차 아이콘 마퀴
- 8단계 아이콘 카드 무한 마퀴 (S4 컴포넌트 재사용)
- 헤드라인: **"상담부터 오픈까지, 빠르고 투명하게"**
- 8단계: 창업상담 → 상권분석 → 점포계약 → 가맹계약 → 인테리어 → 교육 → 오픈준비 → 그랜드오픈

### S11. 창업비용 테이블
- 헤드라인: **"소자본으로 시작하는 OO펍"** / 서브: "거품 없는 창업비용, 항목별로 투명하게 공개합니다"
- 15/20/25평 테이블, 합계 행 accent 강조. **아래 수치는 전부 더미 — `[확인필요]`**

| 구분 | 내용 | 15평 | 20평 | 25평 |
|---|---|---|---|---|
| 가맹비 | 상호·상표 사용, 영업권 보호 | 300 | 300 | 300 |
| 교육비 | 조리·운영·서비스 교육 | 200 | 200 | 200 |
| 인테리어 | 내부공사 일체 | 2,400 | 3,200 | 4,000 |
| 주방집기·비품 | 냉장/냉동, 튀김기, 생맥주 시스템, 의탁자 등 | 2,500 | 2,700 | 2,900 |
| 간판·사인물 | 전면간판, 내외부 사인 | 800 | 800 | 800 |
| 음향 | 스피커, 앰프 | 100 | 100 | 100 |
| **합계** | | **6,300** | **7,300** | **8,300** |

- 각주: "※ 별도비용: 철거, 냉난방기, 가스, 승압, 외부공사 등 / 점포 환경에 따라 변동될 수 있습니다."
- 모바일: 평형 탭 전환 방식

### S12. 배너 롤링 + 클로징
- 언론보도/제휴 배너 롤링 슬라이더 (admin 배너 관리 연동, 더미 배너 3개로 구현)
- 클로징 카피 이미지 1장: "당신의 성공창업, OO펍이 함께합니다"

### 공통 고정 요소
- **하단 고정 가맹문의 인라인 폼 바** (위 ★★ 핵심 레이아웃 5번 참조 — 모달 아님, 전 페이지 상시 노출): 성함 / 연락처 / 가맹희망지역(시도 16개) / 유입경로(네이버·인스타그램·페이스북·유튜브·구글·매장방문·지인소개) / 개인정보처리방침 동의 체크 / 레드 문의하기 버튼 → POST /inquiry/submit. 개인정보처리방침 텍스트 클릭 시 약관 레이어.
- 레이어 팝업: admin 등록 이미지 최대 3개, "오늘하루 닫기" 쿠키(24h), 모바일 대응
- 우측 고정 풀페이지 도트 내비 (PC 전용) + Go Top

---

## 3. 서브 페이지

| 페이지 | 구성 |
|---|---|
| 브랜드 | 풀비주얼 + 브랜드 스토리 3블록 (탄생 배경 → 맥주 철학 → 가맹 철학) `[확인필요: 스토리 원문]` |
| 연혁 | 세로 타임라인 좌우 교차 카드 (history 테이블 연동, 더미 5건) |
| 메뉴판 미리보기 | 전체 메뉴 그리드 or 메뉴판 이미지 |
| 메뉴 리스트 | 카테고리 탭 + 카드 그리드 + 상세 모달 (S6 재사용) |
| 인테리어 | masonry 갤러리 + 라이트박스 |
| 매장찾기 | 지역 셀렉트 필터 + 매장 카드(지점명/주소/전화) + 카카오맵 embed `[확인필요: 카카오맵 API 키]` |
| 개설절차 | 8단계 스텝 카드 세로 나열 (번호/아이콘/설명) |
| 창업비용 | S11 확장 + 상담 CTA |
| 창업문의 | 퀵폼 확장판 (문의내용 textarea 추가) + 접수완료 페이지 |
| 창업소식 | 게시판 리스트/뷰 (notice) |
| 약관류 | admin 에디터로 수정 가능한 문서 페이지 |

## 4. 공통 컴포넌트 (바닐라 JS)

1. **ScrollReveal** — IntersectionObserver, `data-reveal="up|left|right|fade"` `data-delay`, threshold 0.15, 1회 실행
2. **InfiniteMarquee** — 세트 3복제 + `translateX(-33.333%)` keyframes, `data-speed` `data-direction`, hover pause (S4/S7/S10 공용)
3. **Modal** — 딤 + Ajax 주입(`data-modal-url`), ESC/딤 닫기, body scroll lock
4. **LayerPopup** — 쿠키 `popup_close_{id}` 24h
5. **StickyHeader** — scrollY > 80 → `.scrolled`
6. **Parallax** — `data-parallax="0.3"`, 모바일 비활성
- 공통 모션: duration 0.6~0.8s, `cubic-bezier(0.22,1,0.36,1)`, stagger 0.1s. Swiper 외 라이브러리 금지.

## 5. 디자인 시스템 (벤치마크 톤 그대로 — 블랙/화이트/레드)

```css
:root {
  --color-red:   #E8262B;  /* 유일한 포인트 — CTA, 강조 span, 뱃지, 합계 행 */
  --color-black: #111111;  /* 헤드라인, 헤더 텍스트 */
  --color-dark:  #0D0D0F;  /* 다크 섹션 배경 (실사진 위 딤 처리) */
  --color-paper: #EDEAE4;  /* 라이트 섹션 배경 (종이 텍스처) */
  --yellow:      #FFD34D;  /* 다크 배경 위 보조 강조 (뱃지 내부 등) */
  --font-display: 'GmarketSans' 또는 'Paperlogy', 'Pretendard', sans-serif;
  --font-body: 'Pretendard', sans-serif;
  --hd-h: 96px; --form-h: 96px; --container: 1240px;
}
```

- **헤더는 화이트, 본문 섹션은 다크(실사 풀블리드)/페이퍼 교차**. 라운드 최소화(각진 사각형 위주) — 벤치마크는 radius 거의 없음.
- 헤드라인: 초대형 블랙 볼드 `clamp(38px, 4.8vw, 72px)` + **레드 부분 강조** + 필요 시 레드 배경 흰 글씨 스트립 카피
- 다크 섹션 특수효과: **네온 글로우 텍스트**(text-shadow 다중), 레드 네온 보더, 찢긴 종이 엣지(clip-path 지그재그), 기울어진 도면/사진(rotate 소량)
- 사진 톤: 다크 배경 실사 풀블리드 + 하단 그라데이션 딤 → 텍스트 가독 확보
- CTA 버튼: 레드 배경 + 화이트 볼드, radius 0~2px

## 6. DB 스키마 (MariaDB)

```sql
menu_category (id, name, sort, is_active)
menu (id, category_id, name, name_en, price, description,
      badge ENUM('new','best','signature',''), image, detail_image, sort, is_active, created_at)
interior (id, title, image, thumb, sort, created_at)
story (id, branch_name, title, summary, content TEXT, thumb, sort, is_active, created_at)
store (id, name, region, address, phone, lat, lng, is_active, created_at)
inquiry (id, name, phone, region, channel, message, agree_privacy,
         status ENUM('new','contacted','closed'), memo, ip, created_at)
banner (id, image, link, sort, is_active)
popup (id, image, link, is_active, start_date, end_date)
notice (id, title, content TEXT, hit, created_at)
history (id, year, month, content, sort)
site_config (cfg_key, cfg_value)
admin_user (id, username, password_hash, created_at)
```

## 7. 백엔드 (Flask)

- Blueprints: `main`, `brand`, `menu`, `interior`, `store`, `startup`, `board`, `inquiry`, `admin`
- 가맹문의: 서버 검증(전화 정규식, 필수값, 동의 체크) + honeypot 필드 + IP rate limit(1분 3회) + 접수 시 관리자 이메일 알림 (알림톡 확장 대비 인터페이스 분리)
- 업로드: `/static/uploads/{board}/`, Pillow 썸네일 자동 생성 (인테리어 480px, 메뉴 600px)
- 관리자: 세션 로그인, 전 게시판 CRUD + 문의 상태관리(신규/상담중/종료) + 약관 에디터 + site_config 편집
- 시드 데이터: 더미 메뉴 10종·카테고리 8종·인테리어 12장·스토리 4건·매장 3건·연혁 5건 자동 삽입 스크립트 (`seed.py`)

## 8. SEO (네이버 중심)

- 전 페이지 SSR + 개별 title/description (site_config 기반)
- OG 태그 풀세팅, 네이버 서치어드바이저 verification 자리
- sitemap.xml / robots.txt 자동 생성 라우트
- JSON-LD: Organization + 매장별 LocalBusiness
- sr-only h1: "{브랜드명} — 맥주 프랜차이즈 창업"
- 이미지 alt 규칙: "{브랜드명} {대상}" 필수 포함

## 9. config/brand.py

콘텐츠 치환 지점 전체가 이 파일 하나에 집중. 별첨 `config/brand.py` 참조 — **자료 수급 후 이 파일만 수정하면 사이트 전체 반영.**

## 10. 작업 순서

1. 스캐폴딩 + DB 마이그레이션 + seed.py + brand.py 로딩
2. 공통 레이아웃 (header/footer/GNB/플로팅/모달/팝업) + 디자인 토큰
3. 공통 JS 4종 (ScrollReveal/Marquee/Modal/LayerPopup)
4. 메인 S1~S12 (더미 이미지: 로컬 placeholder 생성, 외부 핫링크 금지)
5. 서브 페이지 11종 → 6. 관리자 CRUD → 7. SEO/반응형 QA (375/768/1280px) → 8. 배포 세팅 (gunicorn+nginx conf)

## 11. 금지/주의

- 리얼펍 원본 이미지·카피 복사 금지 (이 문서의 카피만 사용)
- 더미 이미지는 단색+텍스트 placeholder 를 Pillow로 생성해 사용 (외부 이미지 핫링크 금지)
- `[확인필요]` 값은 그대로 두고 구현 — 추후 config/DB 수정으로만 교체되도록 하드코딩 금지
